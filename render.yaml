services:
  - type: web
    name: color_palette_tool
    runtime: python
    buildCommand: "python3 -m pip install -r requirements.txt"
    startCommand: "gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.13"
